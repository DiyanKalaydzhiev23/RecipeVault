@model RecipeVault.ViewModels.RecipeViewModel

@{
    ViewData["Title"] = "Create Recipe";
}

<h2>Create Recipe</h2>

<form asp-action="Create" method="post" enctype="multipart/form-data" class="container mt-4">
    @Html.AntiForgeryToken()

    @await Html.PartialAsync("_RecipeFormPartial", Model)

    <div class="mb-3">
        <button class="w-100 p-2 custom-button" type="submit">Create</button>
    </div>
</form>
